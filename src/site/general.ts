export interface GeneralInformation {
  contactEmail: string;
  companyName: string;
  replyToEmail: string;
  phoneNumber: string;
  contactPhoneNumber?: string;
  address: string;
  googlePlaceId?: string;
} 